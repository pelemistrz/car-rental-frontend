import { ReservationHistory } from './reservation-history';

describe('ReservationHistory', () => {
  it('should create an instance', () => {
    expect(new ReservationHistory()).toBeTruthy();
  });
});
